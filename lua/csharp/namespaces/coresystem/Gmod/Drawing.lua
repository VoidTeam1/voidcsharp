-- Surface
local surfaceTbl = table.Copy(surface)
local Surface = System.define("Void.GLua.Surface", surfaceTbl)

-- Draw
local drawTbl = table.Copy(draw)
local Draw = System.define("Void.GLua.Draw", drawTbl)