-- Surface
local surfaceTbl = table.Copy(surface)
local Surface = System.define("VoidSharp.Surface", surfaceTbl)

-- Draw
local drawTbl = table.Copy(draw)
local Draw = System.define("VoidSharp.Draw", drawTbl)